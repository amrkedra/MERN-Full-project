all:
  children:

    AWS-Jenkins-Server:
      hosts:
        aws-jenkins:
          ansible_host: "<AWS_EC2_IP>"
          ansible_user: ec2-user # or another username based on the AMI used
          ansible_ssh_private_key_file: ./key-bahrain.pem

    # masters:
    #   hosts:
    #     master:
    #       ansible_host: 192.168.1.4
    #       ansible_user: amrkedra
    #       ansible_ssh_private_key_file: ./vm-rsa-key

    # workers:
    #   hosts:
    #     worker:
    #       ansible_host: 192.168.1.9
    #       ansible_user: amrkedra
    #       ansible_ssh_private_key_file: ./vm-rsa-key

    # k8s_cluster:
    #   children:
    #     masters: {}
    #     workers: {}
